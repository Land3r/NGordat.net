/**
 * List of events that can be emitted by common components.
 */

// Emitted when 'value' is changed.
export const VALUE_CHANGED = 'value_changed'

// Emitted when 'value' is invalid or valid based on validation prop.
export const VALUE_VALID = 'value_valid'
