/**
 * Quasar's notify default config for errors.
 */
export const NotifyError = {
  color: 'negative',
  textColor: 'white',
  message: null,
  position: 'bottom',
  actions: [{ icon: 'close', color: 'white' }]
}

/**
 * Quasar's notify default config for warnings.
 */
export const NotifyWarning = {
  color: 'warning',
  textColor: 'white',
  message: null,
  position: 'bottom',
  actions: [{ icon: 'close', color: 'white' }]
}

/**
 * Quasar's notify default config for infos.
 */
export const NotifyInfo = {
  color: 'primary',
  textColor: 'white',
  message: null,
  position: 'bottom'
}

/**
 * Quasar's notify default config for successes.
 */
export const NotifySuccess = {
  color: 'positive',
  textColor: 'white',
  message: null,
  position: 'bottom'
}
